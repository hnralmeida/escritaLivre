void adicionaAluno (TLista *l, string nomeCurso, string nomeTurma){
	string nome;
	int encontrado;
	TAluno *atual = l->ini;
	TAluno *novo = (TAluno*)malloc(sizeof(TAluno));
	
	printf("\nColoque o nome do aluno: ");
	fflush(stdin);
	puts(novo->nome);
	printf("\nColoque o sexo do aluno: ");
	fflush(stdin);
	puts(novo->sexo);
	novo->prox= NULL;
	novo->ante= NULL;
	
	
	
	
}
